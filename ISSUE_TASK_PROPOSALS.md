# RFQIP 이슈 기반 작업 제안

코드베이스(`index.html`)를 빠르게 점검해 아래 4개의 작업을 제안합니다.

## 1) 오탈자 수정 작업
**작업명:** `이익율` → `이익률` 표기 통일

- **발견 근거:** 테이블 헤더/지표 라벨에 `이익율` 표기가 반복 사용됨.
- **영향:** 사용자 신뢰도 저하 및 용어 일관성 문제.
- **제안 작업:** 화면 전체 문자열에서 `이익율`을 `이익률`로 일괄 치환하고, 용어집(있다면)과 동기화.
- **완료 기준:** UI 전 영역 검색 시 `이익율` 문자열이 남아있지 않음.

## 2) 버그 수정 작업
**작업명:** Google Fonts URL 파손 수정

- **발견 근거:** `<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900견적 버전&display=swap">` 형태로 URL에 불필요 문자열이 삽입됨.
- **영향:** 폰트 로딩 실패 또는 weight 파싱 오류 가능성.
- **제안 작업:** 폰트 링크를 정상 포맷(`...wght@400;700;900&display=swap`)으로 복구하고 네트워크 패널에서 200 응답 확인.
- **완료 기준:** 브라우저 개발자도구 Network에서 폰트 CSS 요청 성공, 콘솔 경고/오류 없음.

## 3) 코드 주석/문서 불일치 수정 작업
**작업명:** 도넛 섹션 주석(`/* donut placeholder */`)과 실제 구현 상태 정합화

- **발견 근거:** CSS 주석은 "placeholder"로 기술하지만 UI는 도넛 SVG/범례를 실제 컴포넌트처럼 사용.
- **영향:** 유지보수자가 해당 섹션을 임시 코드로 오인할 수 있음.
- **제안 작업:**
  - 실제 운영 UI라면 주석을 `/* donut chart */` 등으로 변경
  - 진짜 placeholder 의도라면 TODO/스펙 링크를 주석에 명시
- **완료 기준:** 주석이 실제 구현 의도와 일치하고, 팀 컨벤션에 맞는 설명으로 정리됨.

## 4) 테스트 개선 작업
**작업명:** 핵심 UI 동작 스모크 테스트 추가 (최소 4케이스)

- **배경:** 현재 저장소에 자동화 테스트가 없음.
- **제안 테스트 범위:**
  1. 초기 렌더 시 `dashboard` 페이지 활성 상태 검증
  2. `nav('rfqnew')` 호출 시 step 1 활성 및 배너 숨김 검증
  3. `openDetail(id)` 호출 시 breadcrumb와 RFQ ID 동기화 검증
  4. `calcSum()`에 샘플 입력 후 매출/이익/이익률 계산값 검증
- **권장 도구:** Playwright(브라우저 E2E) 또는 Vitest + jsdom(함수 단위)
- **완료 기준:** CI에서 스모크 테스트 자동 실행, 실패 시 회귀 즉시 감지.
